# E2E 稽核（2026-09-09）

線上：`https://life-circuit-chengwan.davidliang986.workers.dev/?debug=1`  
版本：`ad6acb1` 加除錯面板後，現場再修自動通關。

工具：browser-use MCP 實點；CLI 因本機 Chrome「允許遠端除錯」擋下，改接獨立 Chrome 失敗後用 MCP。子代理同步在跑。

## 過關紀錄

| 段 | 操作 | 結果 |
|---|---|---|
| 標題 | `?debug=1` 左側有傳送／跳過／自動通關 | 通過。設定鈕仍寫「寬鬆時間已開」（舊存檔） |
| 新開 | 開始冒險 → 確定覆寫 | 通過 |
| 簡報 | 三頁地圖，可跳過 | 通過。林博士字仍貼在研究站上 |
| P-S00 | 跳過簡報進場 | 通過。黃燈路、樓、閘可讀。雙重「小岑」牌、除錯列擋住 WASD |
| 自動通關 | 按 F10 | 數秒內跳到 P-S03，然後**卡死**：角色掉出地圖，目標 140m→535m，畫面只剩人物與虛空 |
| F9 跳過 | 從 P-S03 跳到 P-S04 | 通過。閘下室內可認，鏡頭偏高 |
| 跳 C1-S01 | 除錯按鈕 | 通過。磚樓＋窗，河港不再是空地板 |

## 已修（下一版）

- 新遊戲強制 `relaxedTimer = false`，不再複製舊存檔的寬鬆開。
- 自動通關每關只傳一次、2.6 秒仍在就跳關，避免在多個互動物件間狂傳把人甩出地圖。
- 傳送到目標旁 0.85m、Y=0，不踩進碰撞體。

## 仍在的問題

1. **誠實玩 P-S01 推箱／P-S02 脈衝** 這次沒完整走完（自動通關跳過）。子代理若有誠實路徑會補。
2. 除錯面板蓋住觸控 WASD。
3. 舊存檔標題仍顯示寬鬆已開；新開一局後應關。
4. P-S03 自動傳送會掉虛空（上一版；修後需再驗）。
5. HUD 元件互相遮（目標晶片、觸控、圖例）。
